# Local tooling / caches — never commit
.claude/
.wrangler/
worker/.wrangler/
**/*.local.json
node_modules/
__pycache__/
*.pyc

# Internal strategy docs — keep private (not served on the public site)
marketing/
