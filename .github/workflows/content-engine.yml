name: Content Engine (nightly local-SEO page)

# Generates ONE local-SEO page per run. Pages that pass the built-in compliance +
# structure scan are AUTO-MERGED to main and go live; any page that trips a check is
# left as a [needs-human] DRAFT PR for review (those never auto-merge).
#
# Self-contained: calls the Anthropic API directly (no Claude GitHub App needed)
# and opens the PR with the built-in GITHUB_TOKEN via the gh CLI.
#
# One-time setup required before this works:
#   1. Add repo secret ANTHROPIC_API_KEY  (Settings > Secrets and variables > Actions).
#   2. Settings > Actions > General > Workflow permissions:
#        - "Read and write permissions"
#        - check "Allow GitHub Actions to create and approve pull requests".
#
# Test it before trusting the schedule: Actions tab > this workflow > "Run workflow".

on:
  schedule:
    # 07:00 UTC ≈ 2–3am US Eastern. Adjust to taste (cron is UTC).
    - cron: "0 7 * * *"
  workflow_dispatch: {}

# Don't let two runs collide if one is slow.
concurrency:
  group: content-engine
  cancel-in-progress: false

jobs:
  generate-page:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write        # create the branch + commit
      pull-requests: write    # open the draft PR
    env:
      GH_TOKEN: ${{ github.token }}                    # lets `gh pr create` authenticate
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      CONTENT_ENGINE_MODEL: claude-sonnet-4-6          # ~5x cheaper than Opus; plenty for these human-reviewed template pages
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate one page and open a draft PR
        run: python .content-engine/run.py
